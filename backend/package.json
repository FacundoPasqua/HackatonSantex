{
  "name": "test-results-api",
  "version": "1.0.0",
  "description": "FastAPI backend for test results",
  "engines": {
    "python": "3.12.0"
  },
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}

